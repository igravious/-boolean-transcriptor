<%=
    if defined? thumbnail
        #why doesn't locals: local_assigns work?
        render partial: "items/thumbnail", object: item, locals: local_assigns
    else
        render partial: "items/default", object: item, locals: local_assigns
    end
%>
