<%= form_for(note) do |f| %>
  <%# f.error_messages %>
  <%= render partial: "form_for_#{note.type}", locals: {f: f, note: note} %>
  <%= f.hidden_field :type %>
  <%= f.hidden_field :scan_id %>
  <%= f.hidden_field :item_id %>
  
  <!-- or how do we look and where did we come from? -->
  <input id="aspect" name="aspect" type="hidden" value="<%= params['aspect'] %>" />
  <!-- TODO DRY -->
  <input id="whence" name="whence" type="hidden" value="<%= params['whence'] %>" />
  <!-- TODO DRY -->
  <input id="q" name="q" type="hidden" value="<%= params['q'] %>" />
<% end %>
