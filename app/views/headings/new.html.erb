
<%= render 'breadcrumb' %>

<h2>Create a new <span class="hot-cinnamon">index</span> term for the collection</h2>
<hr>

<%= debug params %>

<%- content_for(:head) do -%>
    <%# javascript_include_tag "jquery.dropdown", "data-turbolinks-track" => TURBO %>
    <%# stylesheet_link_tag "jquery.dropdown", media: "all", "data-turbolinks-track" => TURBO %>
<%- end -%>

<!-- put this in _form ? -->
<!-- can it figure out url: -->
<%= form_for @heading, url: the_index_headings_path do |f| %>
    <!-- prolly can extract Heading.new from f -->
    <%= render partial: 'new_panel', locals: {heading: @heading, locator: Locator.new, f: f} %>
    <p>
        <input type="hidden" id="params_scan_id" name="scan_id" value="<%= params['scan_id'] %>" />
        <%# f.hidden_field params[:scan_id] %>
        <%= f.submit %>
    </p>
<% end %>
